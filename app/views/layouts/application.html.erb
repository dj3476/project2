<!DOCTYPE html>
<html>
  <head>
    <title>Project2</title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">    
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>

    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
  </head>

  <body>
  	<nav class="navbar navbar-default navigationBar">
      <div class="container-fluid mainNavigation"> 
        <div class="navbar-header navigatonHead">
          <button type="button" class="navbar-toggle collapsed" data-toggle"collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <!-- Link to about page that redirects back to current age -->
          <a class="navbar-brand" href="<%= static_pages_index_path %>">Burts Sporting Goods</a>
        </div>

        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <%= form_tag("/products", method: "get", class: "navbar-form searchBar") do %>
            <div class="form-group">
              <%= label_tag(:q, "Search for:") %>
              <%= text_field_tag(:q, nil, class: "form-control", size: "50%") %>
              <%= submit_tag("Search", class: "btn btn-primary") %>
            </div>
          <% end %>
          <ul class="nav navbar-nav navigationLinks">
            <li class="active"><a href="<%= static_pages_index_path %>">Home<span class="sr-only">(current)</span></a></li>
            <li><a href="<%= static_pages_landing_page_path %>">Shop</a></li>
            <li><a href="<%= static_pages_contact_path %>">Contact Us</a></li>
          </ul>
        </div><!--end navigation links-->
      </div><!--end main navigation-->
  	</nav>

    <%= yield %>

    <footer class="stickyFooter">
      <div class="container-fluid">
    	 &copy; 2016 Burts Sporting Goods
      </div>
    </footer>
  </body>
</html>
